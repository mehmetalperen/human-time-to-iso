{
  "name": "human-time-to-iso",
  "version": "1.0.0",
  "description": "API to convert natural language appointment requests to ISO datetime format",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node dev-server.js",
    "deploy": "vercel --prod",
    "test": "node test.js"
  },
  "dependencies": {
    "chrono-node": "^2.7.7",
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
